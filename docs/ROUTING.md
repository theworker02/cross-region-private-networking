# Routing

Policies: LOWEST_LATENCY, MOST_STABLE, REGION_PREFERRED, COST_AWARE, BALANCED. Explainability via `fabric explain-route`. Flap control: hysteresis, min improvement, min lifetime. Affinity sticky by client key. Standby promotion + drain for failover.

No storage-aware routing.
