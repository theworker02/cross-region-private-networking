# Benchmarking

```bash
cargo run -p fabric-cli -- benchmark
cargo run -p fabric-cli -- sim 100
```

Results: `PHASE4_BENCHMARKS.json`. Always preserve SIMULATED vs REAL labels. Do not fabricate improvements vs public HTTPS — default harness reports additive overhead.
