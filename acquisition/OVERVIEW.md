# Overview

Cross-Region Private Networking is a proprietary encrypted **service fabric** that gives multi-region applications stable private names (`*.fabric.internal`) and identity-aware policy while bridging region-scoped private networks via authenticated fabric nodes.

Independent project; **not affiliated with Render**.
